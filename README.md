# 株価テクニカル分析ツール

**「トヨタ」「ドル円」「ビットコイン」**のように日本語で入れると株価を取ってきて、
移動平均・ボリンジャー・一目均衡表・OSGFC・Lorentzian Classification などのテクニカル指標、
空売り残高、信用残、資金フロー、ニュースを1画面で見るツールです。
**PCとスマホのお気に入り・履歴は Google ドライブ経由で自動同期されます。**

## ファイル

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体(これ1つで動きます) |
| `worker.js` | 自分専用の中継サーバー(任意・置くと速く・私的になります) |
| `中継サーバーの作り方.md` | 上を置くための手順(5分) |
| `manifest.json` | ホーム画面に追加するための設定 |
| `sw.js` | オフラインで開くためのキャッシュ |
| `icon-192.png` / `icon-512.png` | アイコン |

**`worker.js` 以外の5つを同じ階層に置いてください。** サブフォルダに分けると動きません。

`index.html` だけをパソコンで直接開いても(`file://`)、指標も空売りデータもふつうに使えます。
そのときは Google ドライブ同期とオフライン機能だけが無効になり、
設定の持ち運びは「ファイルに書き出す」「コードをコピー」で行います。

## 置く場所

同期とオフラインを使うなら、どこかに **https で公開**する必要があります。
GitHub Pages / Netlify / Cloudflare Pages のいずれかにフォルダごとアップロードしてください。
無料の範囲で足ります。

## 同期の設定(初回だけ)

1. Google Cloud Console で OAuth クライアント(ウェブアプリケーション)を開く
2. **承認済みの JavaScript 生成元**に、公開したURLの生成元(例 `https://xxxx.github.io`)を追加する
   - ここを自分のドメインだけに絞ってください。空のままだと誰でもこのクライアントIDを使えます
3. OAuth 同意画面のスコープに `https://www.googleapis.com/auth/drive.appdata` を追加する
4. PC でアプリを開き、いちばん下の「PC ↔ スマホで共用」→「Googleドライブに接続」
5. スマホで同じURLを開き、同じ操作をする

**Orange Card と同じ生成元に置くなら、クライアントIDはそのまま使い回せます。**
`drive.appdata` はクライアント単位の隠しフォルダで、保存ファイル名が
(`orange-card-data.json` と `ta-tool-sync.json` で)違うため、記録が混ざることはありません。
別のIDを使いたいときは、画面の「準備と、うまくいかないとき」の中の欄に貼って「IDを保存」を押してください。

設定は Drive の「アプリ専用データ領域」に入ります。マイドライブには表示されず、
このアプリ以外からは読めません。ファイルを選ぶ操作は不要です。

同期のタイミングは、**アプリを開いたとき / ★を押したとき / 別のタブから戻ってきたとき**です
(戻ってきたときは前回から1分以上たっている場合だけ)。「今すぐ同期」でいつでも手動実行できます。

## 何を見られるか

入力欄に日本語で入れると候補が出ます。コードを覚えている必要はありません。

| 種類 | 入力例 |
|---|---|
| 日本株 | `トヨタ` `とよた` `ソニー` `ユニクロ` `三菱UFJ` `7203` `285A` — 主要597銘柄を内蔵(日経225 / JPX日経400 / 日経500)。2024年以降の英数字コード(`285A` など)も引けます |
| 海外株 | `AAPL` `MSFT` — ティッカーで。**FINRAの空売り比率**と、**SEC EDGAR の決算数値(PER・PBR・ROE)**も取れます |
| 為替 | `ドル円` `ユーロ円` `ポンド円` `豪ドル円` `ユーロドル` ほか15通貨ペア |
| 暗号資産 | `ビットコイン` `イーサリアム` `リップル` — 円建て/ドル建ての両方 |
| 商品 | `金` `原油` `銀` `プラチナ` `天然ガス` `銅` `小麦` ほか |
| 指数 | `日経平均` `S&P500` `ナスダック` `ダウ` `VIX` `ハンセン` ほか |
| 米国債利回り | `米10年債` `米30年債` `米5年債` `米13週` |
| ETF | `SPY` `QQQ` `VOO` `GLD` `TLT` / 国内なら `1306` `1321` `1540` ほか |

内蔵の表にない銘柄は、入力して「分析」を押すと Yahoo の検索にかけます(そのときだけ通信します)。

**投資信託(eMAXIS Slim など)は扱えません。** Yahoo Finance の米国APIが日本の公募投信を持っていないためです。
`2559`(全世界株ETF)や `1306`(TOPIX ETF)など、上場しているETFで代用してください。
**TOPIX指数そのものもYahooに無い**ため、`1306` を使っています。

## 中継サーバーを自分のものにする(おすすめ)

株価もニュースも、ブラウザから直接は取れません(CORS)。既定では無料の公開中継サーバーを
経由していますが、**混んでいると時間切れになり、見ている銘柄がその運営者に渡ります。**

同梱の `worker.js` を Cloudflare Workers に置くと、その両方が解決します。無料枠で足ります。

**パソコンの Chrome か Firefox で**行ってください(Cloudflareの編集画面はSafariでは動きません)。

1. アプリの「通信(中継サーバー)」→「worker.js を保存」(または同梱のファイルを開く)して全部コピー
2. https://workers.cloudflare.com/playground を開く
3. 左のコード欄を **Ctrl+A → Ctrl+V** で貼り替える
4. 右上の **Deploy**(初回はログインとサブドメイン登録を求められます)
5. 出てきた `https://….workers.dev` をアプリの欄に貼って「保存して確認」

詳しい手順は同梱の **中継サーバーの作り方.md** にあります。

中継してよい相手はコード内で7ホストに限定してあるので、URLを知られても
他人が別サイトへの踏み台に使うことはできません。
**転送(リダイレクト)先も1ホップずつ同じ判定にかけます** — 許可ホストが
別のサイトへ飛ばしてきても、そこには付いていきません。
`worker.js` の先頭にある `ALLOW_ORIGIN` を自分の公開URLにすると、さらに自分のページ専用になります。

**独自ドメインに置く場合**は、`index.html` の先頭付近にある `connect-src` にそのドメインを
足してください(`workers.dev` のURLならそのままで使えます)。アプリ側でも警告を出します。

## お気に入り一覧・アラート・シグナルの成績

- **空売り残高は「報告者ごとの最新報告の合計」**です。JPXの日次ファイルは
  *その日に提出された報告*であって残高一覧ではないため、単純に合計すると
  報告の無い日が0になり、合計も実際よりはるかに小さく出ます(v19以前はそうでした)。
  いまは報告者ごとに最新値を持ち越して合計しています。<br>
  **0.5%未満になると報告義務が消える**ので、そこから下は追跡できません。また、
  さかのぼる日数が少ないと以前から積まれている残高が抜けます。投資情報サイトの
  数字と揃えたいときは「半年分」以上を取得してください
- **残高を持ち越す単位は「報告義務者＋委託者」**です。同じ運用会社が複数の
  ファンドについて別々に報告することがあり、会社名だけで数えると後の行が前の行を
  上書きして合計が小さく出ます(v20以前はそうでした)。一覧では委託者も表示します
- **割合のスケールはファイル単位で判定**しています。JPXのファイルは割合を
  0.0292(＝2.92%)という小数で持ちセルの書式で%を付けているのですが、
  1セルだけを見ると「0.45」が *0.45%* なのか *45%* なのか決められません
  (0.5%を割った解消報告は実際に0.45のような値になります)。ファイル全体で
  %書式が1つでもあるか、全部の値が0.5未満か、で決めています。v20以前の版が
  保存したキャッシュも読み込み時に補正します
- **空売り残高は13ヶ月分**取れます。JPXが月別ページで公開しているので、
  「半年分」「1年分」で月別ページをたどって集めます。1日1ファイルなので1年分は約240件・数分かかります。
  取得済みの日付は飛ばすため、途中で止めて後日続けても構いません
- **決算発表予定日**はJPXの予定表から取れます(予定日のみ)。チャートに縦線が入ります。
  **過去の発表日と月次売上は扱っていません** — 無料で機械的に取れる出所が無いためです
- **信用残の1年分は取れません**。JPXの銘柄別はPDFのみ、日証金は当日スナップショットのみです
- **逆日歩(品貸料率)**は日本証券金融の当日の一覧(`shina.csv`)から取れます。
  出ないときは、その銘柄に逆日歩が付いていない(貸株が不足していない)か、貸借銘柄ではありません
- **日経のページ**(信用残の推移・逆日歩と貸借残・株主優待)へのリンクをカードに置いています。
  日経のサイトは `robots.txt` で自動取得を断っているので、**中身の取り込みはしていません**。
  リンクを押してご自身のブラウザで開く形にしてあります
- **お気に入り一覧** … ★の銘柄をまとめて取得し、現在値・前日比・RSI・25日線乖離・クロスを1つの表に。
  見出しを押すと並べ替え、行を押すとその銘柄を開きます
- **アラート** … 移動平均クロス / RSI / 価格で通知。**ページを開いている間だけ動きます**
  (サーバーを持たないため)。一度鳴ったら、条件から外れるまで鳴り続けません
- **このシグナルは効いているのか** … 各シグナルが過去に出たあと何本後にどうなったかを集計します。
  **「どの足でもいい適当な地点」から同じ期間を見た中央値と比べ**、それを超えていなければ
  そのシグナルはこの銘柄では何も見つけていません。10回未満の行は薄く表示します。
  いずれも手数料・スリッページ抜き、表示中の期間への後付けです

## 足種

`1分 / 5分 / 15分 / 30分 / 1時間 / 4時間 / 8時間 / 日足 / 週足 / 月足`

**4時間足と8時間足は提供元に存在しません。**1時間足をまとめて作っています。
そのことは見出しに「1時間足4本ぶんをまとめて作成」と表示します。
まとめる範囲は**同じ日の中だけ**で、日をまたぐ足は作りません
(東証は6時間立会なので、4時間足は1日あたり「4時間+2時間」の2本になります)。

## パネルの高さを変える

各パネルの**下の余白を上下にドラッグ**すると、そのパネルだけ高さが変わります。
ダブルクリックで元に戻ります。価格パネルも同じように変えられます。
高さは端末ごとに保存されます。

## 開いたときに自動で取れるもの

日本株を開くと、**空売り残高(直近10日)・信用残・決算発表予定**を自動で取りにいきます
(1銘柄1日1回まで)。空売り残高のカードのチェックボックスで切れます。

## 自動更新が動いているか分からないとき

更新表示に **取得回数**と**変化の有無**が出ます。「取得12回・変化なし」なら、
きちんと動いていて相場が動いていないだけです。
見出しには **取引時間中 / 取引時間外**も出ます — 止まって見える理由はたいていこれです。

ストリームは **受信件数**を表示します。「接続済み・配信なし」が続く場合、
その銘柄は配信対象外か、市場が閉じています。45秒無音だとそう表示します。

## 全期間を見るときの注意

**「全期間」は日足では返ってきません。** 提供元が長期の日足を持っていないためで、
週足か月足に丸められたデータが返ります。この場合ツールは自動的に足種の表示を実際の足に切り替え、
指標も**その足で計算し直します**(月足なら「25ヶ月線」と表示されます)。
「日足の全期間」を見せるふりをして月足の数字を出すことはしません。

## 1枚にまとめて書き出す

画面下の「1枚にまとめて書き出す」で、チャート・シグナル判定・指標の数値・直近20本を
**A4 1ページ**にまとめられます。印刷ダイアログで「PDFに保存」を選んでください。

**数値は画像ではなく文字として入ります。** AIにPDFを読ませたとき、チャートの見た目ではなく
実際の値(RSI、MACD、一目の転換/基準、空売り残高…)をそのまま読み取れます。
チャットに直接貼るなら「文字だけコピー」のほうが確実です。

## 何が同期されて、何がされないか

| | 同期 | 衝突したとき |
|---|---|---|
| お気に入り | する | 銘柄ごとに新しい操作が勝つ。**外した操作も伝わります** |
| 閲覧履歴 | する | 銘柄ごとに新しい閲覧時刻が勝つ |
| テーマ(明/暗) | する | 新しく変えたほうが勝つ |
| タブ構成・指標のオンオフ | 送るが、勝手には適用しない | 「別端末の画面を再現」を押したときだけ入れ替わります |
| 空売り・信用・決算の数値の蓄積データ | 「空売り・信用・決算の数値も含める」をオンにしたときだけ | 日付ごとに足し合わせ(消えません) |
| 画面の段組み(1列/2列/全画面) | **しない** | 見ている画面の性質なので、端末ごとの設定のままにしています |
| アラートの設定 | **しない** | 端末ごとの設定です(通知は開いている端末でしか鳴らないため) |
| 株価そのもの | **しない** | 取り直せるので入れていません(同期ファイルは1KB前後です) |

★を外したことも同期されます。片方の端末で外せば、もう片方も次の同期で消えます。
消したあとで同じ銘柄をもう一度★にすれば、そのまま両方に戻ります。

## iPhone を使う場合

**必ず共有ボタンから「ホーム画面に追加」してください。**
Safari は、ホーム画面に追加していないサイトの保存データを約7日で消すことがあります。
同期を設定してあれば全部は失われませんが、追加しておくほうが確実です。

## 手で持ち運ぶ場合(ネット接続なしでも可)

Google に接続したくないときは、同じ画面の下半分にある

- **ファイルに書き出す / 読み込む** … JSONファイルとして受け渡し
- **コードをコピー / 貼り付け** … 500文字程度の文字列。LINEやメールで送れます
- **同期先ファイルを選ぶ**(PCのみ) … iCloud Drive などの同期フォルダ内のファイルを覚えて、次からは1クリックで上書き

を使ってください。取り込み方は「合併(お気に入りを足す)」と「置き換え(タブ構成も再現)」から選べます。

## 動作を自分で確かめる

URL の末尾に `#selftest` を付けて開くと、**その端末・そのブラウザで実際に関数を動かした**
135件のテスト結果が出ます(指標の計算、タイムゾーン、XSS、通信先の許可リスト、保存と復元、端末間の同期)。
画面下の「動作テストを実行」ボタンでも同じことができます。

## 同期がおかしくなったとき

「同期できませんでした」が続き、**「同期ファイルを作り直す」**ボタンが出たときは、
Drive側のファイルが壊れています(別のプログラムに上書きされた、古いバックアップを戻した等)。
このボタンで削除して、その端末の内容から作り直せます。もう一方の端末にしか無いお気に入りは、
その端末が次に同期したときに戻ります。

## 注意

- 投資判断の助言ではありません。表示される「シグナル」は、指標の定義どおりの機械的な判定です
- 株価は Yahoo Finance / Stooq から、CORS中継サーバ経由で取得しています。
  中継サーバは第三者のものなので、**見ている銘柄コードはその運営者に見えます**。
  気になる場合は CSV 読み込みでお使いください
- チャートの縦線は**株式分割**(実線)と**配当落ち**(点線)です。「分割・配当」でオフにできます
- 空売り残高は JPX の日次ファイル(`YYYYMMDD_Short_Positions.xls`)から、
  信用残は日本証券金融の全銘柄CSV(`zandaka.csv`)、逆日歩は同じく `shina.csv` から取得します。
  取得できないときはファイル読み込み(.xls/.csv)に切り替えてください
- **JPXの公開は13ヶ月分**です。それ以前に報告されたまま一度も更新されていない残高は
  この集計に入らないので、何年も蓄積している投資情報サイトより小さく出ることがあります。
  「1年分」まで取り込むと差は縮みます
- 「このシグナルは効いているのか」の**「的中率」は、そのシグナルが主張した向きに動いた割合**です。
  売りシグナルなら「下がった割合」を出します(上がった割合ではありません)。
  v20以前は売りシグナルでも「上がった割合」を出していて、外れたときほど数字が高く見えていました
- ニュースは Google ニュースRSSを中継サーバー経由で読みます。混んでいると時間切れになります。
  もう一度押すか、表示される「Googleニュースで開く」からどうぞ
- **PER・PBR・ROE・配当利回りは、お使いの方が入れた決算の数値と株価から計算した結果**です。
  本ツールは決算そのものを配信していません。入れた数値が古ければ、指標も古いままになります

## v39 — 株価指標(PER・PBR・ROE)

**決算の数値を4つ入れておくと、あとは株価が動くたびに指標を計算し直します。**

株価はこのツールがすでに持っているので、足りないのは決算側の数字だけです。

| 入れるもの | そこから出るもの |
|---|---|
| EPS(1株当たり純利益) | **PER** ＝ 株価 ÷ EPS |
| BPS(1株当たり純資産) | **PBR** ＝ 株価 ÷ BPS |
| 1株配当(年間) | **配当利回り** ＝ 配当 ÷ 株価 |
| 自己資本比率 | そのまま |
| — | **ROE** ＝ EPS ÷ BPS(別に入れる必要はありません) |

入れ替えるのは決算のたび、年に4回だけです。入れ方は3つあります。

1. **手で入れる** — 4項目。分かるものだけで構いません
2. **株価サイトからコピーして貼り付ける** — Yahoo!ファイナンス・株探・みんかぶなどの指標欄。
   EPSやBPSが載っていなくても、PER・PBRと株価から逆算します。
   **取り込む前に「ページから読んだ値」を必ず確認してください**
3. **米国株は SEC EDGAR から自動** — 無料・APIキー不要。10年分の四半期をまとめて取り込みます

### チャートに「PERの推移」が出ます

チップの **「PER / PBRの推移」** で出ます。横線は**この銘柄自身の過去の四分位**(安・中央・高)で、
市場平均の15倍ではありません。チートシートに書いた
「市場平均ではなく、同業他社と、その会社自身の過去と比べる」を実際にやる部分です。

- 赤字でPERが引けない銘柄は自動でPBRに切り替わります
- 決算を入れた日より前は、いちばん古い決算の数値を当てはめた**参考値**なので、薄い破線にしています
- 決算が1期しか入っていないときは四分位を出しません(PERの分布が株価の分布そのものになるため)

### 「指標の見かた」をカードの中に入れました

PER・PBR・ROE・EPS・配当利回り・自己資本比率を、4つの問い(割安か / 稼げているか /
還元しているか / 潰れないか)に整理したものです。**PBR ＝ PER × ROE** の関係と、
「PBR1倍未満＝割安」「PER15倍が基準」「自己資本比率40%」といった目安の落とし穴も書いてあります。

### 気をつけていること

- 本ツールは決算の数値そのものを配信していません。**入れた数値が古ければ、指標も古いままです**
- 米国株の自動取得は提出書類をそのまま計算した値で、証券会社の調整後の数字とは一致しないことがあります
- 同じ決算期に複数の提出があるときは**最初の提出**を採ります(訂正版はその時点では公表されていないため)
- 株式分割があった銘柄では、株価が分割後に合わせて過去まで調整されて配信されるので、
  決算書の1株当たりの数字も**チャートの分割イベントに合わせて割り直して**います。
  分割イベントが取れない読み込み方(CSV・Stooq)では、この直しは行いません
- 第4四半期を単独で開示しない米国企業のために、**通期から3四半期を引いて**第4四半期を作っています。
  四半期に穴があるときは足しません(15か月を1年として足すよりは、出さないほうが安全です)

### 更新するときに2つだけ

- **`worker.js` を Cloudflare に貼り直してください。** SEC を中継先に追加しました
  (v35以降まだ貼り直していない場合は、FINRA の追加も一緒に入ります)
- **両方の端末を v39 に差し替えてから同期してください。** 古い版は決算の数値を知らないので、
  古い版が同期すると共有ファイルからその部分だけ消してしまいます(v35 で FINRA を足したときと同じです)

### v39 の最終チェックで直したもの

出したあとに、蓄積したデータが消える経路が2つ見つかったので直しました。
どちらも「タイミング次第」で起きるものです。

- **同期や取り込みの最中に「保存」を押すと、その銘柄の記録がまるごと消えることがありました。**
  保存は順番待ちに並ぶのですが、順番が来たときに<b>その時点の手元の値</b>を書いていたため、
  待っているあいだに読み込みが走ると空を書き、待っているあいだに同期が書き足したぶんは消えていました。
  保存する値をその場で確定させ、書く直前にディスクを読んで突き合わせるようにしました。
  空売り残高と信用残高の保存も同じ作りだったので、まとめて直してあります。
- **リロードのあとに2枚目のタブを押すと落ちて、前の銘柄の決算が別の銘柄の記録として保存されることがありました。**
  復元では開いていたタブぶんしか指標を組み立てていないのに、そのまま表示しようとしていたのが原因です。
  無ければその場で作るようにし、あわせて<b>「手元の記録がどの銘柄のものか」を持たせて、
  合わないときは何も表示しない</b>ようにしました。
- 「桁の合わない N件を外しました」の直しが、書き戻しのときに元に戻っていたのも直しました
  (直っていない記録が同期で他の端末に渡っていました)。
- 「消す」がlocalStorageに文字列の `null` を書くだけだったのを、鍵ごと消すようにしました。
- 指標の貼り付けに長さの上限(20万字・2000行)を付けました。ページを丸ごと貼ると1秒以上固まっていました。

セルフテストは 145 → **189件**になりました。

## v38 — 為替チャートの見え方と、遅延の表示

**為替や指数のチャートが縦長になる問題を直しました**

3つ重なっていました。

1. **出来高パネルが空のまま場所を取っていた** — 為替・指数・VIX・債券利回りは
   出来高が配信されないので、74pxの空の箱が毎回入っていました。出来高が
   1本も無いときはパネルごと出しません。そのぶん価格パネルが広くなります
2. **一目均衡表の雲が右側を食いすぎていた** — 雲は26本先まで伸びるので、
   3ヶ月の為替チャートでは**右半分近くが空白**になり、ローソクが左に
   押し込まれていました。実データの本数に対して**半分まで**に抑えました
   (実測で右側21〜23%に安定)
3. **縦の拡大が最小0.25倍まで許されていた** — この値だと軸がデータの**4倍**に
   広がり、ローソクが真ん中の帯に潰れます。いただいたスクリーンショットの
   140〜180という目盛りはこれです。スマホで縦スクロールしようとして
   2本指が触れると起きます

3つ目は**戻し方が無いのが本当の問題**だったので、縦が等倍でないときだけ
**「縦の拡大を戻す」ボタン**を移動ボタンの横に出すようにしました。
最小値も0.5倍までに狭めています(それでも軸はデータの2倍)。

**遅延の表示を price のすぐ下に出すようにしました**

小さい注釈だけでなく、値段の真下にバッジを出します。同じ20分でも
足種によって意味が変わるので、強さを変えています。

| 足種 | 表示 |
|---|---|
| 日足・週足 | 灰色「20分遅れ・リアルタイムではありません」 |
| 分足・時間足 | 黄色 |
| 分足で10分以上 | **赤「20分遅れ・直近20分は未反映」** |

場が閉じているときは出しません(引け後の「遅れ」は意味がないため)。

## v37 — ニュースを新しい順に

ニュースが Google の関連度順のまま並んでいたため、去年の記事が今日の記事の上に
来ることがありました。**新しい順に並べ替え**、さらに **直近7日 → 直近30日 → 期間指定なし**
の順に探すようにしました。7日以内に記事がない銘柄でもパネルが空にならず、
そのときは「直近30日(直近7日に記事がなかったため広げています)」と理由を出します。

以前は「関連度上位12件」を先に切り出してから表示していたので、
**新しい記事がそもそも12件の中に入っていない**ことがありました。
いまは取得したものを全部並べ替えてから12件に切っています。

**信用残の取り込みについて**

銘柄別の信用残を無料で機械的に取れる出所はありません(東証は週次のPDFのみ、
JPXの公式API J-Quants では月3,300円のスタンダードプラン以上)。
一方で、**このツールにはもともと「表を貼り付けて取り込む」機能があります。**
日経などのページで週次の表をなぞってコピーし、貼り付け欄に貼るだけで取り込めます。
日付・信用買残・信用売残の列があれば自動で判別し、単位(株・倍)や前週比の列が
混ざっていても読み取れることを確認しました。外部リンク欄の説明にもこの手順を
書き足しています。

## v36 — 監査で見つかった34件の修正

コード全体を通した監査を行い、**34件**を直しました。重いものから。

**1. 値動きの止まった銘柄でタブが20秒フリーズし、落ちていました(致命的)**

目盛りを作る関数が、刻み幅が数値の分解能より小さくなると
**同じ値に足し続けて無限に回り**ます。整理銘柄・監理銘柄・売買停止銘柄のように
何週間も同じ値段が続くと、移動平均やボリンジャーの丸め誤差だけが幅として残り、
そこがちょうど危険帯になります。実測で **18秒フリーズ → メモリ553MB → エラー**。
全パネルが同じ関数を使うので、価格軸に限りません。

**2. 同期が旧データを「0%」に固定し、復旧不能にしていました(致命的)**

古い版が保存した空売り残高は割合を小数(0.0199)で持っています。画面では
自動で100倍して正しく出るのですが、**同期はディスクの中身をそのまま送りながら
「変換済み」の印だけ付けて**いました。受け取った側は変換しないので、
全部の値が0.5%を下回り「返済済み」と解釈されて消えます。印が付いた後は
どの経路でも直りません。

**3. 設定ファイルの取り込みが信用残の履歴を丸ごと上書きしていました(致命的)**

空売り残高は日付ごとに統合しているのに、**信用残だけ生の上書き**でした。
実測で40点 → 1点。信用残は再取得する手段がないので、消えたら終わりです。

**4. 設定コード1本で不具合を起こせる状態でした**

設定の取り込みで、需給データだけ検証を素通ししていました。細工した設定コードを
読み込ませると、ページ全体の動作をおかしくできます。設定コードを人からもらって
使う場合に効くので、塞ぎました。

**そのほかの主なもの**

- 空のタブに切り替えると、前の銘柄の空売り・決算がサンプルチャートに残っていました
- 米国株のFINRA履歴が**同期にも書き出しにも入っていません**でした
  (チェックボックスは「含める」と書いてあるのに)
- **日足以外を表示していると、アラートが別の指標で鳴っていました**。
  5分足ではプリセットがMA9/MA21に変わるのに、ラベルは「MA5がMA25を上抜け」のまま。
  しかも監視リスト側と取り合って、更新のたびに永久に再通知していました
- 自動更新が毎回Lorentzianまで作り直し、スマホで30秒ごとに約2秒固まっていました
- 同期の銘柄数上限が80で、**コード番号の大きい銘柄と米国株が必ず削られて**いました
- クロール中に同期が走ると、取得した日が両方から消えていました
- v34で入れた**決算の縦線が、予定日には一度も描かれていません**でした
  (既存の足がある日しか描けていなかった)
- 「消す」を押してもカードの中身が残り、消えたか判別できませんでした
- 貼り付けた表と逆日歩が銘柄を変えても残り、別銘柄に保存されていました
- 保存に失敗しても「保存済み」と表示していました
- 保存領域があふれたとき、書いた値が読み戻せず設定が巻き戻っていました
- 縦に拡大すると現在値の値札が他のパネルの上や画面外に出ていました

修正後にもう一度敵対的レビューをかけ、**修正が原因の回帰10件**も直しています
(自動更新の指標引き継ぎで足がずれる、「置き換え」が置き換えでなくなる、
ラベルが枠に収まっているのに消える、など)。

内蔵セルフテストは 145/145 合格です。

## v35 — 米国株の空売り比率(FINRA)

米国株でも需給が見られるようになりました。空売りカードの取得ボタンが、
表示中の銘柄に合わせて**JPXかFINRAかを自動で切り替えます**。

**ただし、日本の「空売り残高」とは別の統計です。** ここを混ぜると誤読するので、
画面でもはっきり分けています。

| | 日本(JPX) | 米国(FINRA) |
|---|---|---|
| 中身 | 発行済株式に対する**残高**の割合 | その日の報告出来高に占める**空売りの割合** |
| 性質 | 積み上がっている量(ストック) | その日の流れ(フロー) |
| 頻度 | 日次・0.5%以上のみ | 日次・当日18時(米東部)以降 |

FINRAの比率は、**流動性のある銘柄なら平常時から40〜50%あります。**
マーケットメイカーが買い注文に応じるときも一旦は空売りになるためで、
**半分が弱気という意味ではありません。** そのため判定は絶対値では出さず、
**その銘柄自身の中央値との差**で見るようにしました(「平常の範囲です」など)。

グラフの軸ラベル・凡例・ツールチップ・判定・書き出しのすべてで、
米国側は「空売り**比率**」、日本側は「空売り**残高**」と呼び分けています。

- 指数・為替・商品・暗号資産は対象外です(FINRAのファイルに載りません)。
  ボタンを押しても通信は発生せず、理由を表示します
- 米国株の**信用残に相当する無料データはありません**。逆日歩・決算予定も日本のみです
- 自動取得は日本株のみのままです。FINRAのファイルは1件が全米国銘柄ぶんで大きいため、
  銘柄を開いただけで落としにいくことはしません
- 中継サーバーを自分で立てている方は、**`worker.js` を貼り直してください**。
  取得先の許可リストに `cdn.finra.org` を追加しています

あわせて、米国株のニュース検索が日本語の「株価」を付けていたため
ほとんど何も出なかったのを直しました。

## v34 — 追加した3つの機能

### 1. アラートの通知が iPhone でも出るようになりました

これまで通知は `new Notification()` で出していましたが、**iPhone/iPad の Safari は
この書き方に対応していません**(タブでも、ホーム画面に追加したアプリでも)。
つまり iPhone では、通知を許可しても一度も鳴っていませんでした。
Service Worker 経由の出し方に変えたので、iPhone でも通知が出ます。
通知をタップするとこのアプリに戻ります。

**ただし、閉じている間は動きません。** 仕組み上どうにもならない部分なので、
アラートを追加したときに、その端末で実際に何ができるかをその場で表示するようにしました。
iPhone で Safari のタブのまま使っている場合は「ホーム画面に追加してください」と出ます。

| 状態 | 通知 |
|---|---|
| アプリを開いて見ている | 出ます |
| 他のアプリに切り替えた | Chrome は数十秒〜1分遅れ、iPhone はほぼ止まります |
| 閉じた / タブを消した | **出ません** |

値動きを絶対に逃したくない場面では、証券会社アプリのアラートをお使いください。
あちらは本物のプッシュ基盤を持っています。

### 2. 「上位足の流れ」を追加しました

5分足を見ているときの日足、日足を見ているときの週足、というように、
**ひとつ上の時間軸のトレンド**をチャートと判定リストに出します。
「5分足は買いだが日足は下降中」という、いちばんよくある負け方を潰すためのものです。

- 判定リストでは、トレンドのすぐ下に **一致しているか・逆らっているか**を出します
- チャートには上位足の平均線を**階段状**に引きます。上位足の足が確定するまで値は動かないので、
  なめらかな線にすると持っていない情報があるように見えてしまうためです
- **通信は増えません。** 手元にある足を畳んで作っています。CSV読み込みだけでも動きます
- **確定した足だけ**を見ます。形成中の足を混ぜると、更新のたびに過去の線が書き換わります
  (いわゆるリペイント)。系列を途中で切っても先頭が1本も変わらないことを確認しています

期間が短いと上位足の本数が足りません。そのときは線を引かず「データが足りません」と出ます。

### 3. 決算発表が近いと、判定リストの先頭に出ます

5日以内なら注意色、14日以内なら参考として、**いちばん上**に出ます。
下に20項目並ぶテクニカルの一致より先に目に入るべき情報だからです。
決算をまたぐと、下の判定とは無関係に窓を開けて動きます。

あわせて、**分割・配当が1件も無い銘柄でチャートに決算の縦線が出ていなかった**不具合も直しました
(縦線を描く関数が「イベントが空なら何もしない」で先に抜けていて、決算線はその後ろにありました)。
分足チャートや指数・FXでは配当データが空になりがちなので、ほとんどの場面で出ていませんでした。

## v33 — Lorentzian を本家準拠に

Lorentzian Classification を、公開されているオリジナル(TradingView / jdehorty 版)の
**コードに合わせて**組み直しました。TradingView の表示と数字が揃います。

距離の式・特徴量5本・近傍の選び方(距離のしきい値を上げながら拾う独特の手順)は
もともと一致していました。合っていなかったのは次の3点です。

| | 以前 | 本家 |
|---|---|---|
| 学習ラベル | 未来4本の値動き | **直近4本の値動きを反転** |
| 近傍の間引き | 4の倍数**だけ**(1/4) | 4の倍数を**除く**(3/4) |
| レジームフィルタ | 上昇トレンド専用 | 方向を問わない |

**この指標は順張りではなく、逆張り寄りのモデルです。** オリジナルは
「次の4本の方向を予測する」と書いてありますが、コードは直近4本の値動きを
反転して学習しています。RSIやCCIと組み合わせると「安値圏なら買い」に働きます。
以前の版は説明文どおりに実装してあったため、事実上**逆向きの指標**でした
(相関 −0.577)。同様に「4本ごとに間引く」という注釈も、コードは逆で
4の倍数を捨てています。**どちらもコードの側に合わせました。**

あわせて本家に揃えた細かい点:

- CCI の入力を終値に(以前はHLC3)、ADX特徴量の余計な平滑化を削除
- ADXフィルタの期間を20→14(特徴量側は20のまま。本家がそうなっています)
- カーネル回帰の項数を27本に固定、EMA/SMAフィルタの条件を本家の形に

**▲▼の意味が変わりました。** 以前は「シグナルが変わった点」すべてでしたが、
本家に合わせて**カーネルの向きが一致したエントリだけ**を出します。
サンプルデータでは115個→46個に減り、そのぶん的中率の表示は 44%→65% になります
(いずれもインサンプルで、売買推奨ではありません)。
判定リストの「近傍N件の投票」の N はほぼ常に 8 になります(以前は平均4.9)。

なお本家には「系列の終端を知らないと決まらない探索開始位置」があり、
これは過去のバーの値が未来に依存することになるため**複製していません**。
2000本以下では本家と完全一致、それ以上でも配列の温まり方が違うだけです。
未来参照が無いことは、系列を途中で切って先頭が変わらないことで確認しています。

## v32 で直したこと

**同期すると信用取引残高が消えることがありました**

同期でやりとりするデータから、「これは信用残なのか貸借残なのか」という
印(`kind` と各点の `k`)が抜け落ちていました。受け取った側は印が無いので
**出所のファイル名から種別を推測し直します**。このとき `zandaka`(残高)を
日証金のファイルとみなす判定が入っていたため、`zandaka_20260710.csv` の
ような **ごくふつうの証券会社の書き出し**が「貸借残」と誤判定されます。
すると、手元に貯まっていた「信用残」の点が *別の統計のものが混ざった* と
判断されて捨てられ、**6週分の履歴が1点まで削られていました**。

3か所直しました。

- 同期のデータに種別を載せるようにしました(点ごとの `k` も含めて)
- ファイル名からの推測から `zandaka` を外しました。残高は信用残・貸借残の
  **両方**の呼び名なので、種別の判断材料になりません。このアプリが
  貸借残として書き出すものは必ず「日証金」か「貸借」と名乗っています
- **推測が外れても履歴を消さない**ようにしました。全部の点が推測と食い違う
  ときは、間違っているのは点ではなくラベルの方です。以前はそこで全件破棄でした

あわせて、種別の食い違う信用残と貸借残が同じ記録に混ざらないようにし、
一度確定した種別はディスクに書き戻して読むたびに推測し直さないようにしました。

20回同期を往復させて、3点→23点と**履歴が正しく積み上がる**こと、
空売り残高も2件→8件で貯まることを実機で確認しています。

## v31 で直したこと

全体を機械的に検査して見つかった **40件** を直しました。重いものから。

**1. 自動取得が一度も動いていませんでした(致命的)**

`shortBusy` という変数がどこにも宣言されていませんでした。ファイル全体が
`"use strict"` なので、参照した瞬間に例外になります。しかもエラーは
`.catch(() => {})` に吸われるため、画面には何も出ません。
その結果 **空売り残高・日証金の貸借残・決算発表予定の自動取得は、
設定をONにしていても一度も実行されていませんでした。**
手動の「取得」ボタンも、表示はされるものの必ず失敗として扱われていました。

**2. 取得を押すと、貯めた空売り残高が消えることがありました(致命的)**

キャッシュを読む道が2つあり、片方(クローラ側)が古い形式を新しい形式に
直す処理を通っていませんでした。古い版で保存したデータがある銘柄で
「取得」を押すと、**蓄積した日数が新しく取った分だけに置き換わり**、
さらに割合が100分の1のまま「変換済み」の印を付けて保存されるため、
**あとから直せなくなっていました**。Googleドライブ同期の経路にも
同じ入口があり、そちらは同期のたびに起きていました。
いまは読み出し口をひとつにまとめてあります。

**3. 中継サーバーが「オープンプロキシ」になっていました**

許可ホストの判定が取得の *前* に一度きりで、転送の追跡は
ブラウザ任せ(`redirect: 'follow'`)でした。許可した相手が別のサイトへ
飛ばすだけで、**許可していないサイトの中身がそのまま返ります**。
1ホップずつ判定するようにしました。あわせて、
`ALLOW_ORIGIN` を設定してもブラウザ以外(curl など)は素通りだった点と、
`content-length` の無い応答でサイズ上限が効かなかった点も直しました。

**4. 週足・月足に、その日1日分だけのローソクが1本増えていました**

最新値を反映する処理が「日付」で比べていたため、週足・月足では
月の2日目以降・週の火曜以降に **同じ期間のローソクがもう1本**
足されていました。移動平均もRSIも1本ずれ、出来高の棒は1日分だけ立ちます。
足の単位で比べるようにしました。

**5. Apple や Microsoft を開くとJPXを取りに行っていました**

「日本株かどうか」の判定 `/^[0-9A-Z]{4}$/` が `AAPL` `MSFT` `META` `NVDA`
にも当たっていました。Appleを開いただけで、SheetJS(約900KB)と
JPXの日次ファイル最大10本(1本1〜3MB)と日証金のCSV(約3MB)を
取りに行き、**Appleのチャートに残高0%の線が引かれていました**。

**6. 逆に、285A のような新しいコードは引けませんでした**

2024年から発行されている「3桁+英字」のコードに `.T` が付かず、
Yahooでもstooqでも必ず外れていました。証券会社の画面どおりに
入れても「銘柄が見つかりません」になります。

**7. 0.5%を割った解消報告が、ずっと足され続けていました**

画面には「0.5%を下回った報告＝解消として外す」と書いてありましたが、
実際に外していたのは 0 の行だけでした。0.45% のような撤退時の最終報告は
二度と更新されないので、**幻の残高として永久に加算されます**。
撤退が増えるほど合計が実際より大きく出ていました。

**8. Excelで保存したCSVを貼り付けると、桁が壊れて保存されていました**

`"1,234,000"` のような引用符つきの数字を、引用符の中のカンマで
割っていました。見出し行だけは引用符を落とすので列は合ってしまい、
**エラーにも「読み飛ばし」にもならないまま** 信用買残が消え、
信用売残 567,000 が 234 として保存されていました。

**9. 空のタブを開いたまま再読み込みすると、サンプルデータが「前回の続き」になりました**

復元したタブが「まだ空」という印を失っていたため、画面に残っていた
サンプル銘柄の値が、そのタブの中身として保存されていました。
次に開くと **「サンプルデータです」の断り書きなしで架空の株価が出ます**。

**10. Googleドライブ同期が、接続を解除すると二度と動かなくなりました**

トークンを待っている最中に「接続を解除」を押すと、待っていた処理が
解放されないまま捨てられ、同期の待ち行列ごと止まります。
再接続しても直らず、再読み込みするまで同期が死んでいました。
また、起動時の裏の要求が返ってこない間に「接続」を押しても、
**同意画面を出す要求自体が送られていませんでした**。

**設定の持ち運び(ファイル/コード)の取り込み**

- 上限30件を「新しい順に並べる前」にかけていたため、**取り込んだ履歴が
  1件も入らないのに「履歴 30件」と表示**していました
- 相手側で消したお気に入りがこちらに反映されず、**画面には★を出しながら
  Drive には「削除済み」を上げる**という食い違いが残っていました
- 「履歴を消す」を無視していたため、消した履歴が復活し、そのうえ
  次の同期で相手の履歴を消していました

**そのほか**

- 空売り残高の割合を補正するとき委託者名を落としていたため、同じ運用会社の
  複数ファンドが1件に潰れ、合計が小さく出ていました
- `130A` と `130B` を同じ銘柄として集計していました(コードから英字を除いていたため)
- xlsxで保存した日証金の表が、圧縮された中身ではなく生バイト列で判定され、
  週次の信用残と同じ軸に載っていました
- 古い形式の警告が銘柄を変えても消えず、正常なデータにも
  「消して取り直してください」と出続けていました
- 全部の取得に失敗した日も「今日は成功」と記録し、その日いっぱい再試行しませんでした
- 指標のON/OFFで下の判定リストが更新されず、チャートと食い違っていました
- ストリーム更新で新しい足ができるたび、表示本数が1本ずつ増えていました
- 出来高が配信されない銘柄(為替・指数・VIX・債券利回り)で、VWAPが
  株価そのものを、MFIが「50」を、それぞれ実データのように表示していました
- 金額表示の「万円」だけ有効数字が1桁で、14,999円が「1万円」になっていました
- 比較モードで、実際に描かれているローソク足・移動平均の凡例が消えていました
- マウスドラッグと矢印キーでの移動後、◀▶ と位置表示が古いままでした
- 貼り付け欄のフォーカス枠が、未定義の色を参照していたため出ませんでした
- オフライン用のキャッシュが、CDNのエラー応答やWi-Fiのログインページも
  保存していました。一度そうなると **サイトデータを消すまでExcel関連が
  全部壊れます**。本体HTMLも、404や502をキャッシュより優先していました
- 内蔵セルフテストのうち2件が、実際には何も検査せずに合格していました

## v21 で直したこと

パーセントが合わない件を追いかけて、周辺も含めて見直しました。

**空売り残高の数字**

- 割合のスケールをファイル単位で判定するようにしました。1セルだけを見て
  「0.45は小数か%か」を決めていたので、解消報告(0.5%割れ)が混じったファイルで
  0.54 → 54% になり、合計が147%のような値になることがありました
- 同じ運用会社が複数ファンドについて出した報告が、会社名だけで数えていたため
  1件に潰れていました。委託者まで見るようにして、合計が実際の値になります
- 同期でやり取りするときに委託者が落ちていたので、往復すると合計が縮んでいました
- 画面に、日次ファイルの意味(残高一覧ではないこと)と13ヶ月の制限を書きました

**比較チャート**

- 5分足を出しているのに日足しか無い銘柄を重ねると、階段状の線と
  意味のない相関係数が出ていました。足種が違うものは断って理由を出します
- チップを続けて押すと二重に取得して、4本の上限を超えることがありました

**アラート**

- アラートを共有の通知欄に書いていたため、次に何かの読み込みが終わると
  読む前に消えていました。専用の欄に出して、自分で閉じるまで残します
- 同期で来た見覚えのない種類のアラートで、通知処理ごと落ちていました

**操作**

- パネルの区切り線をドラッグしても指から離れていく問題(パネルを広げると
  上の価格パネルが縮んで、区切り線が元の位置に戻っていたため)
- 素早くタップしたときに `setPointerCapture` が例外になり、
  ドラッグの初期化が途中で止まることがありました
- 「今すぐ全部取り直す」が取得中は無反応でした。押せない見た目にして理由を出します

**集計**

- シグナル成績の中央値が、偶数件のときに上側の値になっていました
  (買いは高め、売りは低めに出ます)

## v22 で直したこと・足したこと

**「なんで数日前のデータなの？」**

Yahooの日足は、まだ終わっていない当日を含めずに返してくることがあります。
同じ応答の `meta` には当日の値が入っているのに、チャートだけが1〜2日
遅れて見える状態でした。metaの最終取引を最新の足として取り込みます
(既にある日なら値を更新、無い日なら足を1本足す)。

あわせて、判断できるように<b>3つの時刻</b>を出しました。

- チャートの最新の足の日付
- Yahoo自身が言う最終取引の日時（**取引所の時計**で表示します）
- このページが最後に取りにいった時刻

上2つが同じなら、それが市場の答えです（例: 8月11日は山の日で東証は休場）。
違っていればこのツール側の問題なので、そう分かります。

無料のCORS中継サーバーはURL単位でキャッシュするため、「自動更新」が
同じ応答を読み直していました。毎回変わるパラメータを付けて回避します。

**表を貼り付けて取り込む**

日経のように自動取得を断っているページは、**ご自身のブラウザで表を選択して
コピーし、貼り付け欄に貼る**だけで取り込めるようにしました。CSVに直す必要は
ありません。

- タブ区切り（Webの表をコピーするとこれになります）・カンマ・スペースを読みます
- 見出しの言葉（日付 / 信用買残 / 信用売残 / 信用倍率 / 逆日歩）から列を判別します
- 見出しが無ければ「日付・信用買残・信用売残」の順とみなし、そう表示します
- `2026/08/07` `26/08/07` `08/07` `2026年8月7日` `20260807` を日付として読みます
- 桁区切りのカンマ、`1.2万`、`△1,200`（マイナス）も読みます
- **取り込む前に必ず中身を表示します。**違っていたら押さなければ入りません

**お気に入り**

★の並びが「ボタンが増えただけ」に見えていたので、件数つきの引き出しにして、
コードではなく**会社名**で並べました。「現在値つきの一覧を見る」で
お気に入り一覧の表まで飛びます。「すべて外す」も付けました。

**リンクの置き場所**

株主優待が信用取引残高のカードにあったのを直しました。
「この銘柄のページ」という別のカードを作って、株主優待・Yahoo!ファイナンス・
適時開示・空売り残高のリンクをそちらに移しています。
信用取引残高のカードに残したのは、信用残と逆日歩・貸借残だけです。

**そのほか**

- 「次の寄付 09:00」を取引所の時計で出すようにしました（海外から見ても正しい時刻になります）
- 取引中の足を「終値」と書いていたのを「時点(取引中)」に

## v23 で直したこと

**信用残の数字が合っていなかった** — 原因は2つでした

1. 日経の表は数字のうしろに毎回「（解説）」の列が入ります。見出しが7列、
   数字が4列で、見出しの位置で列を読んでいたため**全部ずれて**いました
   (買残 68.0万株・売残 1,300株・信用倍率 522.77倍 になっていたのはこれです)。
   見出しは、下の数字の列数に合わせてから使うようにしました。
   日経のように**売残が先**に並んでいても取り違えません。
2. **信用残と貸借残を混ぜていました**。
   - 信用残(東証・日経) … 市場全体、週次
   - 貸借残(日証金) … 日証金を通した分だけ、日次

   イオンで 646万株 対 68万株、桁が1つ違います。同じ系列に入れていたので、
   日証金を取得した日にチャートが崖のように落ちていました。
   別々に扱い、日証金の値はカードの下に**並べて**出します(線には引きません)。
   貸借残だけのときはパネル名も「貸借残(融資 / 貸株)」に変わります。

**株価の端数を消していました** — イオン 1,393.5円 が 1,394円 になっていました。
1,000円以上を整数に丸める規則が効いていたためです。日本株は1,000円超で0.5円刻みなので、
**実際にある端数は出す**ようにしました。移動平均のような割り切れない値は増やしません。

**吹き出しが下のチャートで見えなかった** — キャンバスの上端に固定していたので、
パネルを全部出すと画面2〜3枚分の高さになり、下のパネルまでスクロールすると
数字も一緒に画面外へ行っていました。**見えている範囲を追う**ようにしました。
項目が多いときは2列にして、価格チャートを覆わないようにもしています。

**表の貼り付けが別カードだったのを、信用取引残高の中に入れました。**
折りたたみで「表を貼り付けて取り込む」を開くと出ます。

**シグナル判定を整理しました。** 12個の枠を並べるのをやめて、
1行ずつの並びにしました。説明は2行までにして、「説明をひらく」で全文が出ます。

**空売り残高・信用残がページを更新すると消えていた**(v22で報告いただいた件)。
データは消えていませんでした — カードの数字は出たままでしたよね。
消えていたのは**チャートのパネル**で、株価だけから指標を作り直す処理が3か所あり、
そこで外部データの結びつけが外れていました。更新の約0.6秒後に走るので
「更新したら消える」ように見えていました。指標のオン/オフでも同じことが起きていました。
作り直しを1か所に集約して、二度と外れないようにしています。

## v24 で直したこと

**取り込んだ表と信用残が合っていなかった(v23で分けたのに残っていた分)**

v23で信用残と貸借残を分けましたが、**すでに保存されているデータが直っていませんでした**。
以前の版が1つの系列に両方を書き込んでいて、その記録には
どちらの数字なのかが入っていません。表を貼り付けても、
最後に残っていた日証金の1点(8/10 の 68.0万株)がいちばん新しい点として
チャートに引き継がれ、吹き出しが 522.77倍 のままになっていました。

開いたときに**自動で掃除する**ようにしました。

- 保存済みの各点に、どちらの統計かを記録します(以後は混ざりません)
- 記録の無い古いデータは、**桁で見分けます**。週次の信用残が1日だけ
  1/10になって翌週戻ることはないので、中央値から4倍以上離れた点を外します
- 5点以上ないと判定しません(中央値が意味を持たないため)
- 外したときは**カードにそう書きます**。黙って数字を変えません
- 系列の名前も付け直します(残ったのが信用残なら「信用残」に戻ります)
- 日証金だけを貯めている場合は削りません

**「説明をひらく」が効いていないように見えた**

説明を2行に切り詰めていたのですが、ほとんどの説明は元々2行に収まるので
**押しても何も変わりませんでした**。閉じているときは説明を出さないようにして、
ボタンの文字も「各項目の説明を表示」／「説明を隠す」といまの状態を出すようにしました。

**「自動で取り込みしてる？」が分からなかった**

しています。銘柄を開いた直後に1回だけ、
**空売り残高10日分・日証金の貸借残・決算予定**を取りにいきます(1銘柄1日1回まで)。
分からないままなのが良くないので、チェックボックスの下に
「最後の自動取得 8/12 22:25(空売り残高10日分・決算予定)」のように出すようにしました。
まだのとき、切っているときもそう書きます。

## v25 で直したこと

**価格チャートのすぐ下に日付を出しました**

時間軸は一番下に1つだけで、サブパネルを何枚も出すとローソク足から数百ピクセル
離れてしまい、スマホではスクロールしないと日付が読めませんでした。
価格パネルの直下にも日付を入れました(一番下の軸もそのままです)。

**スマホでパネルの高さを変えやすくしました**

- 区切り線の当たり判定が9pxでした。指の目標としては小さすぎます
  (推奨は44px前後)。**タッチ操作のときは22px**に広げました。マウスは9pxのままです
- 区切り線の右端に**掴む場所の目印(横棒)**を描きました。掴めることが分からない、
  というのが一番の問題でした
- そもそもドラッグしなくて済むように、チャートの下に
  **「パネルの高さを変える」** を付けました。表示中のパネルが一覧で並び、
  ﹣ と ＋ (44px)で1押しごとに25%ずつ変わります。今の倍率も%で出ます。
  「全部もとに戻す」もあります

**比較チャートでテクニカルが消えなくなりました**

ドル円などを重ねると、価格パネルが**指数の折れ線2本に置き換わって**いました。
ローソク足も移動平均もボリンジャーも雲も消えるので、比較のために
テクニカルを捨てている状態でした。

「2軸チャートは使わない」という方針は変えずに、**縦軸を指数にしたうえで、
ローソク足も指標も同じ倍率で置き直して**同じ軸に描くようにしました。
形はそのまま、比較の線も同じ軸に乗ります。左上に「表示開始を100とした指数」と
書いてあります。現在値の札は実際の価格のままなので、読みの基準も残ります。

## v26 で直したこと

**中継サーバーの設定などが同期されていませんでした**

Googleドライブに置いていた同期ファイルには、
**お気に入り・閲覧履歴・タブ構成・テーマしか入っていませんでした**。
中継サーバーのURLも、アラートも、各種オン/オフも、そもそも項目が無かったので、
同期しようがない状態でした。追加しました。

**同期するもの**

- お気に入り / 閲覧履歴 / タブ構成 / 指標のオンオフ / テーマ（従来どおり）
- **中継サーバーのURL** — 片方で設定すれば、もう一方の入力欄にも入ります
- **アラート** — 追加・削除・オンオフが両方に届きます。
  片方で消したものが復活しないよう、削除も記録して運びます
- **自動取得のオン/オフ**、**シグナル説明の開閉**
- 空売り・信用の蓄積データ（チェックを入れたときだけ・従来どおり）

**同期しないもの**（意図的にそうしています）

- 株価データ本体 — 取得し直せるので運ぶ意味がありません
- 画面の段組み、**パネルの高さ** — スマホと大きな画面で同じにすると、
  かえって使いにくくなります

**設定ごとに新しい方が残ります。** 例えばスマホで中継サーバーを変え、
PCでシグナル説明を開いた場合、両方とも残ります。
ブロック全体で1つの時刻を持つ作りだと片方が消えてしまうため、
設定ごとに変更時刻を持たせています。

受け取った値は検証します。中継サーバーのURLは形式を確かめてから適用し、
時計が未来にずれている端末の値では上書きされません。
何が届いたかは同期後のメッセージに出ます（例:「設定: 中継サーバー」）。

## v27 で直したこと

**指1本で時間軸を左右に動かせるようになりました**

これまで指1本は「値を読む」だけで、時間軸を動かすには**2本指**が必要でした。
気づきようがなく、片手では厳しい操作でした。

指1本が、そのときの動きに応じて3つに分かれます。

- **横になぞる → 時間軸が動く**
- 縦になぞる → これまでどおり画面のスクロール
- 指を止める → その足の値を読む(止めたあとは指を動かしても読み取りのまま)

向きは最初の数ピクセルで一度だけ決めて、そのジェスチャーの間は変えません
(毎フレーム判定すると、少し斜めになぞったときに移動と読み取りがちらつきます)。
タップだけなら今までどおりその足を読みます。2本指の拡大縮小もそのままです。

**移動ボタンも付けました。** チャートの下に ◀ ▶(52×42px)と
「最新へ」「全体」、それに**いまどこを見ているか**
(例: 2024/12/13 〜 2025/04/28(97本 / 全380本))を出しています。
端まで来たらボタンは無効になります。1押しで半画面分動きます。

**株価が15分ほど遅れている件**

これは配信元の遅れで、ツール側では縮められません。
日本株の無料データは取引所の規定により遅延配信されます。

分からないままなのが良くないので、**2つの時間を分けて表示**するようにしました。

- **配信の遅れ** … 取得した時刻 − その値が付いた時刻。
  これが配信元の遅れです。自動更新やストリームにしても縮みません
- **取得から◯分** … いま − 最後に取りにいった時刻。
  こちらは直せるので、「更新」を30秒ごとかストリームにする案内を出します

どちらも**実測値**です。決め打ちの「20分」ではなく、そのときの実際の差を出すので、
配信元の運用が変わっても表示は正しいままです。
取引時間外は出しません(閉場からの経過時間は遅れではないため)。

## v28 で直したこと

**前日比が前日比になっていませんでした**

15分足で「▲+¥0.5000 (+0.04%)」と出ていた場面は、証券会社では
「-11.0 / -0.79%」でした。**1つ前の「足」との差**を計算して
「前日比」と呼んでいたためです。日足なら同じ意味になりますが、
15分足では「15分前との差」でしかありません。

**前営業日の終値**との差に直しました(配信元がその値を持っています)。
配信に無い場合は、足をさかのぼって前営業日の最後の足を使います
(直前の足は使いません)。

吹き出しでも分けて出します。分足では「前の足から」と「前日比」の両方が並びます。

ついでに **¥0.5000 → ¥0.5**。差額の桁数を差額自身の大きさではなく
**株価の桁**から決めるようにしました(¥1,382.5 の株なら小数1桁)。
ただし差が桁に対して小さすぎて 0 に丸まってしまう場合は桁を増やします
(159.441円の0.015円の動きが「0.0」になっていました)。

**比較チャートに左軸を足しました**

「比較の軸」で **指数(開始を100)** と **左軸に実数** を選べます。
実数にすると、左の軸が比較先の実際の値(ドル円なら159.44など)、
右の軸が株価になります。ローソク足も指標もそのままです。

- 実数の左軸は**比較が1本のときだけ**です。単位の違うものを1本の軸には載せられません。
  2本以上のときは指数に戻し、その理由を書きます
- 2つの軸の高さの合わせ方は**こちらで決めたもので、意味はありません**。
  そう画面に書いてあります。重なり方ではなく形をご覧ください

比較中の吹き出しが、四本値と指標を全部消して指数2行だけになっていたのも直しました。
比較の値は**追加**で並びます。

**2本指を縦に広げると株価の幅が広がります**

これまでピンチは横方向(期間)だけでした。縦に広げると価格の目盛りが伸び、
値動きが見やすくなります。

- 指の動きの**比率**で判定するので、横のピンチが価格の幅を変えたり、
  縦のピンチが期間を変えたりしません
- ダブルタップで両方とも元に戻ります
- タブごとに覚えるので、切り替えても戻りません

## v30 で直したこと

### 1. 4時間足・8時間足で Lorentzian が出なかった

**東京市場の立会いは5時間**しかありません。1時間足が1日6本なので、
それをまとめた**4時間足は1日2本、8時間足は1日1本**にしかなりません。
つまり同じ「1ヶ月」でも、足種によって本数がまるで違います。

| 足種 | 1ヶ月 | 3ヶ月 | 6ヶ月 | 1年 | 2年 |
|---|---|---|---|---|---|
| 15分足 | 約570本 | — | — | — | — |
| 4時間足 | **44本** | 130本 | 260本 | 490本 | 980本 |
| 8時間足 | — | **65本** | 130本 | 245本 | 490本 |

Lorentzian は**最初の60本を学習に使う**ので、44本では1本も予測が出ません。
そして足種を変えたとき、これまでは**同じ「1ヶ月」を引き継いで**いました。
15分足の1ヶ月から4時間足に切り替えると、ちょうどこの44本の状態に着地します。
**だから15分足では出て、4時間足以降は出ませんでした。**

3つ直しました。

- **足種を変えたときは「同じ日数」ではなく「同じくらいの本数」を引き継ぐ**ようにしました。
  15分足の1ヶ月(570本)から4時間足なら、1ヶ月ではなく**1年**(490本)を選びます。
- **指標が引けない短さは自動では選ばない**ようにしました。一目均衡表の先行スパンBは
  52本＋26本先行で最低78本必要です。これに満たない期間へは自動で飛びません。
  (ボタンは残っているので、自分で選ぶことはできます)
- **それでも本数が足りないときは、パネルの中に理由を書く**ようにしました。
  「44本では予測できません(60本以上必要)」「4時間足は1日約2本です。期間を長くしてください」。
  **空のパネルは故障に見えます。**何を待っているのか画面に書くべきでした。

ついでに、同じ理由で**週足の1年(52本)・月足の2年(24本)も一目均衡表が引けていません**
でした。こちらも自動では選ばれなくなります。

案内の文面も直しました。これまでは「期間を5年か全期間にしてください」と出ていましたが、
**4時間足と8時間足には5年も全期間もありません**(1時間足のデータ上限が約2年のため)。
いま押せるボタンだけを案内します。

### 2. 前日比がまだおかしかった

前回 `chartPreviousClose` をやめたのですが、まだ足りませんでした。
足りなかったのは**「1日1本しかない系列を分足として表示している場合」**です。
このとき「前の市場日」を探して1本余計に遡り、**前々日の終値**と比べていました。

いまは、**その日の足が複数あるときだけ**チャートから前日終値を割り出し、
そうでないときは取得元が申告した前日終値を使います。

|  | 使う値 |
|---|---|
| 15分足で今日の足が26本ある | チャート上の**前の市場日の最後の終値** |
| 1日分しか入っていない | 取得元の `previousClose` |
| 日足・週足・月足 | **1本前の終値** |
| 2年分の範囲の先頭より前の値 | **使いません**(これが +19.76% の原因でした) |

### 3. 更新しても、その回は前の版が出ていました

オフライン用のキャッシュが、本体のHTMLまで
**「まずキャッシュを返して、裏で新しいものを取ってくる」**方式でした。
つまり新しい版を置いても、**開いた回は前の版**が表示され、
もう一度開いて初めて新しくなります。

直したはずのものが直っていない、人によって・時によって挙動が違う、
という見え方の一因です。本体だけは**先にネットワークを見る**ようにしました
(3秒で諦めてキャッシュに切り替えるので、圏外でも今までどおり開きます)。

`Cache-Control: max-age=600` を返すサーバー(GitHub Pages がそうです)では、
ネットワークを見に行っても**ブラウザ自身のキャッシュ**が10分間は古い版を
返します。そこも越えるようにしてあります。

**この版に上げるときだけ、1回だけ手動で更新が必要です。**
古いキャッシュが今のルールで動いているためです。スマホなら、
ページを開いて**下に引っ張って再読み込み**を2回してください。

## v29 で直したこと

「動く時と動かない時がある」を追いかけて、操作まわりを一通り見直しました。
原因は1つではなく、**指を無視する経路が7つ**ありました。

**1. 区切り線がスワイプを飲み込んでいました(いちばん大きい原因)**

パネルの高さを変える当たり判定が、タッチのとき **上下22px × パネルの幅いっぱい**で、
しかもパネルごとに2本の線に付いていました。74pxのサブパネルなら**その6割**が
「高さ変更」になり、そこを横になぞっても時間軸は動きません。
指の位置しだいで動いたり動かなかったりする、というのはこれです。

タッチでは**描いてある取っ手(右端の横棒)の上だけ**を当たり判定にしました。
それ以外は全部チャートなので、どこをなぞっても時間軸が動きます。
マウスは今までどおり線全体で掴めます(カーソルが変わるので狙えるため)。

**2. タップした値がすぐ消えていました**

指を離すとブラウザはそのポインタを「消滅」させ、直後に離脱イベントを出します。
それが、タップで出したばかりの値を**描画される前に**消していました。
0.24秒以上押さえていれば別経路で出るので、
「速くタップすると出ない・長く押すと出る」という分かりにくい挙動でした。
タッチの離脱では消さないようにしました(マウスは今までどおり)。

**3. 別の場所に置いた指がピンチ扱いされていました**

片手持ちで親指が画面のどこかに触れていると、それを2本目の指と数えて
「ピンチ」と判定し、スワイプが拡大縮小になったり固まったりしていました。
チャートの上に乗っている指だけを数えます。

**4. ピンチのあと残った指が死んでいました**

2本指で操作して1本だけ離すと、残った指はどのジェスチャーにも属さず、
一度離して置き直すまで何をしても反応しませんでした。引き取るようにしました。

**5. ピンチ状態が取り残されることがありました**

通知や他アプリへの切り替えでジェスチャーが中断されると「ピンチ中」のまま残り、
1本指の操作が全部素通りして、**再読み込み以外に戻す方法がありません**でした。
中断・離脱・タブ非表示のすべてで解除します。

**6. 足種を変えると期間が全期間に飛んでいました**

1時間足の1ヶ月から日足に変えると、いきなり全期間になっていました。
選べない期間になったときの逃げ道が「一覧の最後＝いちばん長いもの」だったためです。
**いちばん近い長さ**を選ぶようにしました(1ヶ月からなら3ヶ月)。

**7. 重い指標の計算が失敗すると画面全体が固まっていました**

Lorentzianなど計算に時間のかかる指標は、計算中は「取得中…」にして
ボタンを止めます。その計算が失敗すると**元に戻す処理まで届かず**、
再読み込みするまで「分析」が押せないままでした。
失敗してもボタンは戻し、その指標だけ切って、何が起きたか表示します。

**そのほか**

- 移動ボタン(◀▶)が端に来ても無効にならず、押しても何も起きない状態でした
- ボタンで動かしたとき、下の位置表示が古いままでした
- 取っ手を掴んだ瞬間に価格パネルが18pxほど跳ねていました
- 価格パネルが画面に収まる上限に当たっているとき、ドラッグが効かないまま
  内部の倍率だけが上限まで進み、あとで指標を減らすと突然3.5倍になっていました
- 起動直後(前回の続きを読み込み中)に「分析」を押すと、取得結果が
  行き場を失って黙って捨てられていました
