# 中継サーバーの作り方(5分・無料)

株価ツールの「通信(中継サーバー)」に貼るURLを作ります。

**パソコンの Chrome か Firefox で作業してください。**
Cloudflare の編集画面は Safari では動きません。スマホでの貼り付けも現実的ではありません。
一度作れば、そのURLはスマホのツールにも同じものを貼れます。

---

## なぜ作るのか

ブラウザから Yahoo Finance などへ直接アクセスすることはできません(CORS という仕組みで止められます)。
そのため、あいだに「中継サーバー」を挟んでいます。

いまは無料の公開中継サーバーを使っていて、そのせいで

- 混んでいる時間帯は**ニュースなどが時間切れ**になる
- **見ている銘柄がその運営者に渡る**

という2つの問題があります。自分専用のものを1つ置くと、どちらも消えます。
Cloudflare の無料枠は 1日10万リクエストなので、この用途では使い切れません。

---

## 手順

### 1. worker.js を用意する

- 同梱の `worker.js` を、メモ帳などのテキストエディタで開きます
- ツール内の「通信(中継サーバー)」→「置きかた」→「**worker.js を保存**」からも取り出せます
- **Ctrl+A → Ctrl+C で全部コピー**しておきます

### 2. Cloudflare の編集画面を開く

ブラウザで開きます:

```
https://workers.cloudflare.com/playground
```

コードが書かれた画面が出ます。これがそのまま編集画面です。

### 3. 貼り替える

- 左側のコード欄をクリック
- **Ctrl+A** で全部選択 → **Ctrl+V** で `worker.js` を貼り付け(元のコードは消えます)

### 4. Deploy を押す

- 画面右上の **Deploy** ボタンを押します
- 初回はここで **Cloudflareへのログイン(無料のアカウント作成)** を求められます
- 続けて **サブドメインの登録**(`○○.workers.dev` の `○○` を決める)を1回だけ求められます
  - ここで決めた名前が、以後あなたのWorkerすべての住所になります
- Worker の名前を聞かれたら何でも構いません(例: `ta-relay`)

### 5. URLをコピーする

デプロイが終わると、こんなURLが表示されます:

```
https://ta-relay.あなたの名前.workers.dev
```

これをコピーします。
**そのURLをブラウザで開いてみて**「中継サーバーは動いています」と出れば成功です。

### 6. ツールに貼る

- 株価ツールの「**通信(中継サーバー)**」の欄にURLを貼る
- 「**保存して確認**」を押す
- 「つながりました」と出れば完了です

実際に株価が取れるか確かめてから保存するので、
間違ったURLが黙って設定されることはありません。

スマホでも同じURLを同じ欄に貼れば、そちらも自分の中継サーバー経由になります。

---

## もう一段だけ絞りたいとき(任意)

`worker.js` の先頭にこういう行があります。

```js
const ALLOW_ORIGIN = '';
```

ここに**ツールを公開しているURL**を入れると、そのページからしか使えなくなります。

```js
const ALLOW_ORIGIN = 'https://xxxx.github.io';
```

(そのURLは、ツールの「置きかた」の説明文の中にも表示されています)

書き換えたら、もう一度 Playground に貼り直して Deploy してください。

---

## よくあるつまずき

**「つながりませんでした」と出る**
デプロイ直後は反映に数十秒かかることがあります。少し待ってもう一度押してください。
それでも駄目なら、URLをブラウザで直接開いて「中継サーバーは動いています」が出るか確かめてください。

**「このアドレスは通信許可リストに入っていません」と出る**
`workers.dev` 以外の独自ドメインに置いた場合です。
`index.html` の先頭付近にある `connect-src` に、そのドメインを1つ足してください。

**Safari で編集画面が真っ白**
Cloudflare の Playground は Chrome / Firefox のみ対応です。

**元に戻したい**
ツールの「通信」欄で「**使わない**」を押すと、公開の中継サーバーに戻ります。
Cloudflare 側の Worker はそのまま置いておいても、削除しても構いません。

---

## 安全性について

`worker.js` は、中継してよい相手を次の7か所に限定しています。

```
query1.finance.yahoo.com / query2.finance.yahoo.com
stooq.com / news.google.com
jpx.co.jp / www.jpx.co.jp / www.taisyaku.jp
```

ここに載っていない宛先は 403 で断ります。
つまり、URLが第三者に知られても、**他人が任意のサイトへの踏み台として使うことはできません**。
Cookie と認証ヘッダも転送しません。
